[tool.poetry]
name = "poetry-basic-sanity"
version = "0.1.0"
description = "Basic Poetry sanity test - simple dependency resolution"
authors = ["Test <test@example.com>"]

[tool.poetry.dependencies]
python = "^3.9"
requests = "2.31.0"
Django = "2.2.0"
flask = "2.2.5"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
